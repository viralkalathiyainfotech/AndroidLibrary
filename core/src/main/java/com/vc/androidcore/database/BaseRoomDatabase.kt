package com.vc.androidcore.database

import androidx.room.RoomDatabase

/**
 * Marker abstract base class for Room databases, if desired.
 * Applications may also extend [RoomDatabase] directly.
 */
abstract class BaseRoomDatabase : RoomDatabase()
